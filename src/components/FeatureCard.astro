---
const { title, body } = Astro.props;
---
<article class="card">
  <h3 style="font-size:var(--h3); margin:.2rem 0 .4rem 0;">{title}</h3>
  <p style="color:var(--muted); margin:0;">{body}</p>
</article>